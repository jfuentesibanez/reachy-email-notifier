# Reachy Connection
REACHY_IP=localhost
REACHY_PORT=50055

# Gmail API Configuration
# After setting up Gmail API credentials, place your credentials.json file in the project root
# The token will be generated automatically on first run

# Email Check Interval (seconds)
CHECK_INTERVAL=60
